# Vue WebSocket Photoshop

Here you can create shapes, change them and move.

Then you will see the changes on another client

# Before use it you need to start both servers

`cd frontend` and `npm run dev` for frontend

`cd backend` and `node server.js` for backend

# That's all! Have fun!